# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy   
 container app to Azure Web App   
 - Justin-Rainey-Portfolio

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    # You don't need to build if you're reverting to an existing tag
    # Comment out or remove the following steps:
    #- name: Set up Docker Buildx
    #  uses: docker/setup-buildx-action@v2

    #- name: Log in to registry
    #  uses: docker/login-action@v2
    #  with:
    #    registry:   
   #https://index.docker.io/v1/
    #    username: ${{ secrets.DOCKER_USERNAME }}
    #    password: ${{ secrets.DOCKER_PASSWORD }}

    #- name: Build and push container image to registry
    #  uses: docker/build-push-action@v3
    #  with:
    #    push: true
    #    tags: |
    #      index.docker.io/${{ secrets.DOCKER_USERNAME }}/magneto80/rainey_cloud:latest 
    #      index.docker.io/${{ secrets.DOCKER_USERNAME }}/magneto80/rainey_cloud:${{ github.sha }} 
    #    file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build  # Remove this line if you commented out the build job
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name:   
         'Justin-Rainey-Portfolio'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_c8d7290a5d074fad91b2fb8883ec72d5 }}
        images: 'index.docker.io/${{ secrets.DOCKER_USERNAME }}/magneto80/rainey_cloud:0.1.4'  # Specify the 0.1.4 tag
